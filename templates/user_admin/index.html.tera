<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Admin - Task Tracker</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.css" rel="stylesheet" />
</head>
<body class="bg-gray-50 font-sans">
    <div class="container mx-auto px-4 py-8 max-w-4xl">
        <header class="mb-10 flex flex-col md:flex-row justify-between items-start md:items-center space-y-4 md:space-y-0">
            <div class="text-left">
                <h1 class="text-3xl md:text-4xl font-extrabold text-gray-900 mb-2">User Administration</h1>
                <p class="text-base md:text-lg text-gray-600">Manage application users</p>
            </div>
            <div class="flex flex-wrap items-center gap-3 md:gap-4">
                <span class="text-gray-700 font-medium whitespace-nowrap text-sm md:text-base">Hello, <span class="text-blue-600">{{ user.username }}</span></span>
                <a href="/" class="text-xs md:text-sm text-blue-600 hover:underline">Back to Tasks</a>
                <form action="/logout" method="post" class="inline">
                    <button type="submit" class="text-xs md:text-sm text-white bg-gray-600 hover:bg-gray-700 px-3 py-1.5 rounded-lg transition-colors">Logout</button>
                </form>
            </div>
        </header>

        <div class="mb-6 flex justify-end">
            <a href="/user_admin/new" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center transition-colors">Add New User</a>
        </div>

        <div class="bg-white rounded-lg shadow-md overflow-hidden">
            <table class="w-full text-sm text-left text-gray-500">
                <thead class="text-xs text-gray-700 uppercase bg-gray-50">
                    <tr>
                        <th scope="col" class="px-6 py-3">ID</th>
                        <th scope="col" class="px-6 py-3">Username</th>
                        <th scope="col" class="px-6 py-3 text-right">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for u in users %}
                    <tr class="bg-white border-b hover:bg-gray-50">
                        <td class="px-6 py-4">{{ u.id }}</td>
                        <td class="px-6 py-4 font-medium text-gray-900">{{ u.username }}</td>
                        <td class="px-6 py-4 text-right space-x-2">
                            <a href="/user_admin/{{ u.id }}/edit" class="font-medium text-green-600 hover:underline">Edit</a>
                            {% if u.username != "admin" %}
                            <form action="/user_admin/{{ u.id }}/delete" method="post" class="inline">
                                <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
                                <button type="submit" class="font-medium text-red-600 hover:underline" onclick="return confirm('Are you sure you want to delete this user and all their tasks?')">Delete</button>
                            </form>
                            {% endif %}
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
